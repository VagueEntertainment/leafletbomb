import { Meteor } from 'meteor/meteor';
import { Email } from 'meteor/email';

MAIL_URL = 'smtp://ideas:B3V4ug3@vagueentertainment.com:587'

Meteor.startup(() => {
  // code to run on server at startup
});
